
\documentclass[paper=a4, fontsize=11pt]{scrartcl} % A4 paper and 11pt font size

\usepackage[T1]{fontenc} % Use 8-bit encoding that has 256 glyphs
\usepackage{fourier} % Use the Adobe Utopia font for the document - comment this line to return to the LaTeX default
\usepackage[german]{babel} % English language/hyphenation
\usepackage{amsmath,amsfonts,amsthm} % Math packages
\usepackage{hyperref}
\usepackage{lipsum} % Used for inserting dummy 'Lorem ipsum' text into the template
\usepackage[htt]{hyphenat}
\usepackage{sectsty} % Allows customizing section commands
\allsectionsfont{\centering \normalfont\scshape} % Make all sections centered, the default font and small caps
\usepackage{minted}
\usepackage{caption}
\usepackage{svg}
\usepackage{graphicx}
\usepackage[
  backend=biber,
  style=ieee
]{biblatex}
\usepackage{fancyhdr} % Custom headers and footers
\pagestyle{fancyplain} % Makes all pages in the document conform to the custom headers and footers
\fancyhead{} % No page header - if you want one, create it in the same way as the footers below
\fancyfoot[L]{} % Empty left footer
\fancyfoot[C]{} % Empty center footer
\fancyfoot[R]{\thepage} % Page numbering for right footer
\renewcommand{\headrulewidth}{0pt} % Remove header underlines
\renewcommand{\footrulewidth}{0pt} % Remove footer underlines
\setlength{\headheight}{13.6pt} % Customize the height of the header

\numberwithin{equation}{section} % Number equations within sections (i.e. 1.1, 1.2, 2.1, 2.2 instead of 1, 2, 3, 4)
\numberwithin{figure}{section} % Number figures within sections (i.e. 1.1, 1.2, 2.1, 2.2 instead of 1, 2, 3, 4)
\numberwithin{table}{section} % Number tables within sections (i.e. 1.1, 1.2, 2.1, 2.2 instead of 1, 2, 3, 4)

%\setlength\parindent{0pt} % Removes all indentation from paragraphs - comment this line for an assignment with lots of text

%----------------------------------------------------------------------------------------
%	TITLE SECTION
%----------------------------------------------------------------------------------------

\newcommand{\horrule}[1]{\rule{\linewidth}{#1}} % Create horizontal rule command with 1 argument of height

\title{	
\includegraphics[width=0.6\linewidth]{img/Mathnatlogo.png}\\[1cm]
\normalfont \normalsize 
\textsc{Green Compting} \\ 
\textsc{Lehrstuhl für Operating Systems and Distributed Systems} \\ [25pt]
\horrule{0.5pt} \\[0.4cm] 
\huge Projektbericht - EPSA \\ EMA Plugin for Slurm \textit{(but Aware!)} \\ % The assignment title
\horrule{0.5pt} \\[1cm]
\author{Sten Heimbrodt} % Your name

\date{\normalsize\today} % Today's date or a custom date
}

\addbibresource{refs.bib}

\newpage
\begin{document}

\maketitle
\newpage
\tableofcontents
\newpage
\section{Einleitung}
Hochleistungsrechencluster sind ein fundamentaler Bestandteil der modernen Forschungslandschaft, insbesondere in den Computerwissenschaften. Folglich besitzt jede größere Forschungsinstitution meist eigene Rechencluster, auf denen ein breites Feld an Anwendungen
ausgeführt wird. Da in der Regel eine Menge von Personen sich etwaige Computerverbünde teilen, ist es notwendig, eine Cluster Management Software aufzusetzen. Diese übernimmt die Verwaltung der Ressourcen des Clusters. Dabei wird meist Slurm eingesetzt, was die Grundlage für die hier vorliegende Projektarbeit bieten soll. Es wurde eine Erweiterung des EPS-PrEp-Plugins der Slurm-API vorgenommen, die einen tieferen Einblick in den Energieverbrauch einzelner Slurm-Jobs ermöglicht. Das Ergebnis der Arbeit ist ein Programm namens \texttt{awareness\_daemon} sowie eine Erweiterung des ursprünglichen EPS-Plugins um weitere Ausgaben und eine Anpassung der entsprechenden Datenbank. 

Die Lizenzierung wurde aus dem Ursprungs-Github-Repository übernommen und ist somit unter der MIT License lizenziert. Es wurde ein Projekt-Logo erstellt (siehe Abbildung 2.1). Das neue Projekt ist nun im Github-Repository 
\begin{itemize}
    \item \href{https://github.com/gespel/EPSA}{https://github.com/gespel/EPSA} 
\end{itemize}
Die Projektarbeit wurde von Frau Prof. Dr. Schnor und Max Lübke, M.Sc., betreut und im Rahmen des Moduls Green Computing umgesetzt.
\section{Motivation}
Der fortschreitende Klimawandel verlangt es heute mehr denn je zuvor, den Energieverbrauch der Menschheit derart zu verringern, dass ein nachhaltiges Leben auf der Erde auch für spätere Generationen von Menschen möglich sein wird. Das bedeutet, in allen Bereichen des Alltags
, die eng mit dem Energieverbrauch verbunden sind, ein Verständnis zu schaffen, was der tatsächliche Verbrauch ist. 

Menschen neigen dazu, den Alltag derart zu abstrahieren, dass ein Gefühl für den eigenen Energieverbrauch meist verloren geht, und es somit zunehmend schwerer wird, eine Akzeptanz für die benötigten Energiesparmaßnahmen zu schaffen. Auch die Nutzung von extrem energieintensiven Hochleistungsrechnern muss dementsprechend sparsam behandelt werden, da ein Slurm-Job, der alle Ressourcen eines Clusters verwendet, Tausende Watt an Leistung nutzen kann und meist eine relativ lange Laufzeit besitzt. Dementsprechend ist derartige Arbeit einer der Hauptenergieverbraucher, pro Kopf gerechnet. 

Technisch wird ein Job auf solch einem Cluster mittels eines Terminals auf dem Kopfknoten gestartet, bei dem explizit angegeben werden muss, welche Ressourcen verwendet werden sollen. Die Bedienung ist allerdings relativ einfach, und besonders das hochskalieren auf Hunderte, wenn nicht sogar Tausende, CPU-Kerne ist letztendlich nur eine Zahl in einem Shell-Skript. Hier setzt nun das Prinzip der Energy-Awareness an, das allgemein die Zielsetzung hat, ein abstraktes menschliches Gefühl für Energieverbrauch herzustellen, damit eine Grundsatz-Moral seitens der Nutzer*innen entstehen kann. Dazu gibt es unterschiedliche Ansätze. 
\begin{figure}[htbp]
  \centering
  \includesvg{img/epsa-icon}
  \caption{EPSA-Logo (erstellt mittels Claude Opus-5)}
\end{figure}
Der in diesem Projekt verwendete Ansatz nutzt ein Energiemessungs-Framework, um die verbrauchte Energie eines Slurm-Jobs zu sammeln und in einer entsprechenden Datenbank samt Nutzerzuordnung abzulegen. Diese Daten sollen daraufhin genutzt werden, um ein Feedback an den Nutzer zu erstellen, in Form eines Vergleichs mit anderen Energieverbrauchern aus dem regulären Alltag. Außerdem soll es eine DSGVO-konforme Möglichkeit des Vergleichs mit anderen Nutzer*innen geben, sodass auch untereinander verglichen werden kann. Damit soll das erhoffte Ziel, den gesamten Energieverbrauch zu senken, erreicht werden, da entsprechende Slurm-Jobs nur bei tatsächlicher Notwendigkeit angestoßen werden sollen und vor allem auch nur in einer Länge, die wirklich notwendig ist, um die entsprechenden Forschungsfragen beantworten zu können. 

\newpage
\section{Grundlagen}
Für die Slurm-Erweiterung EPSA werden eine Reihe von technischen Hilfsmitteln genutzt. Einerseits wird ein Framework benötigt, um die verbrauchte Energie auf den entsprechenden Rechenknoten zu messen, und andererseits muss es eine Schnittstelle seitens der Clusterverwaltungssoftware geben, damit laufende Jobs mit den Aufrufern assoziiert werden können. Außerdem muss eine Messungsumgebung gestartet werden, die zeitlich an der Ausführung des Jobs gebunden ist und korrekt beendet werden muss. Im folgenden werden diese Hilfsmittel vorgestellt und im Detail erläutert.
\subsection{EMA}
EMA ist ein von der PERFACCT GmbH entwickeltes Framework zur Energiemessung von Anwendungen aus dem Umfeld des wissenschaftlichen Rechnens. Es ist modular aufgebaut und erlaubt somit die Integration verschiedener Hardwarearchitekturen mittels eines Plugin Systems. Dies ist vor allem notwendig, weil sich Rechencluster meist aus heterogener Hardware zusammensetzen und die Ausgabe des Energieverbrauchs, bis auf einige Ausnahmen wie IPMI, Hardware spezifisch ist. Es ist somit wünschenswert, eine programmatische Abstraktion zu schaffen, damit Energiemessungen zentral und vor allem normalisiert aggregiert und ausgewertet werden können.

Es gibt unterschiedliche Möglichkeiten, wie EMA die verbrauchte Energie eines Programms messen kann. Zunächst gibt es die regionsbasierte Energiemessung. Dabei können mittels Präprozessor-Makros Regionen definiert werden, in denen speziell Energiemessungen stattfinden sollen. Im folgenden Codebeispiel ist eine solche Energiemessung implementiert:
\begin{minted}[xleftmargin=20pt,linenos]{c}
int main(void) {
    EMA_init(NULL);
    /* Äußere Region: gesamte Laufzeit, über alle Geräte. */
    EMA_REGION_DECLARE(total);
    EMA_REGION_DEFINE(&total, "total");
    EMA_REGION_BEGIN(total);
    
    for (int it = 0; it < 5; ++it) {
        EMA_REGION_DECLARE(compute);
        EMA_REGION_DEFINE(&compute, "compute");
        EMA_REGION_BEGIN(compute);
        work(/* work args */);
        EMA_REGION_END(compute);

        usleep(/* sleep-Zeit */);
    }
    EMA_REGION_END(total);
    EMA_finalize();
    return 0;
}
\end{minted}
In Zeile 2 wird zunächst die EMA-Laufzeitumgebung initialisiert. Daraufhin werden in Zeile 4 bis 6 und in Zeile 9 bis 11 jeweils zwei ineinander geschachtelte Code-Regionen definiert, in denen die verbrauchte Energie gemessen werden soll. 

Die Unterscheidung zwischen innerer und äußerer Messung erlaubt es, zu differenzieren, ob die Energie eher im Kalkulationsteil des Programms oder in entsprechenden Preprocessing-Schritten, wie beim Einlesen von Daten, verwendet wird, bzw. wie das entsprechende Verhältnis der Arbeitsbereiche aussieht. Außerdem bietet EMA auch das entsprechende Tooling an, um die Laufzeit gesamter Applikationen zu messen. Diese Funktion ist für dieses Projekt wesentlich interessanter, da die Verwaltung unterschiedlicher Messregionen innerhalb der Slurm-Umgebung sehr aufwendig wäre.

Die erhobenen Daten werden als eine CSV-Datei ausgegeben, in der insgesamt 11 Spalten mit den unterschiedlichen Messparametern und Werten abgelegt werden. Die entsprechende Belegung und Erläuterung hierzu ist der Dokumentation von EMA aus dem Github-Repository zu entnehmen.

EMA ist vollständig in C geschrieben und besitzt in der Grundkonfiguration drei Plugins zur Energiemessung für drei Hardware-Plattformen, die im folgenden erläutert werden.
\subsubsection{Intel RAPL}
Intel RAPL ist das zentrale Plugin seitens EMA, wenn es darum geht, Energiemesswerte für die Prozessorlast zu ermitteln. Es basiert auf der namensgebenden Anwendung von Intel und wurde entwickelt, um einerseits Energiemessungen (Energy Reporting) vorzunehmen und andererseits die Energieaufnahme mittels Power Capping zu begrenzen.

Das für dieses Projekt interessantere Energy Reporting besteht aus monoton steigenden Zählern, die die Energiewerte repräsentieren. Somit werden nicht die aktuellen Energieverbräuche ausgegeben, sondern müssen mittels zweier Messpunkte für einen Messbereich errechnet werden und bilden somit das Integral des Stromverbrauchs über einen Zeitraum. Dabei unterscheidet Intel spezifische Regionen des Prozessors und bietet die entsprechenden unterschiedlichen Messwerte an. Diese Regionen werden als Domänen bezeichnet und sind wie folgt definiert:
\begin{itemize}
    \item Package (PKG) ist der gesamte Energieverbrauch des CPU-Sockels auf dem Mainboard.
    \item PP0 misst nur den genauen Energieverbrauch der CPU-Kerne.
    \item PP1 misst nur die ggf. integrierte GPU-Einheit und deren Energieverbrauch.
    \item DRAM gibt an, welche Energie vom Arbeitsspeicher verwendet wurde, der mit dem entsprechenden Sockel der CPU verbunden ist.
    \item PSys misst die gesamte System-on-Chip Architektur und ist vor allem bei mobilen Prozessoren im Einsatz.
\end{itemize}
Grundsätzlich hängen die angebotenen Domänen stark von der jeweiligen Plattform bzw. der Architektur des Modells ab. In dem folgenden Projekt wird vor allem mit der Package-Domäne gearbeitet, da für den gesamten Energieverbrauch insbesondere der Verbrauch des CPU-Sockels von interesse ist.

Intel RAPL bietet mehrere Möglichkeiten, auf die erhobenen Daten zugreifen zu können. Einerseits lässt sich direkt auf ein MSR-Register mittels Linuxkernel zugreifen. EMA hingegen verwendet den zweiten angebotenen Ansatz direkt aus dem Linuxkernel mittels sysfs unter dem Pfad \texttt{/sys/class/powercap/intel-rapl*/.../energy\_uj} Daten auszulesen. Außerdem gibt es entsprechende Perf-Erweiterungen. Zusammenfassend gilt jedoch, dass alle drei Wege letztendlich identisch sind und lediglich das vom Prozessor selbst befüllte MSR-Register auslesen. Die zeitliche Auflösung von RAPL liegt bei 1 ms.
\subsubsection{Nvidia NVML}
Nvidia NVML verfolgt ein sehr ähnliches Prinzip wie Intel RAPL. Es bringt allerdings weitaus mehr Limitationen mit sich, was sicherlich in einer Linie mit der geschäftlichen Praxis seitens Nvidias steht. Im Unterschied zu RAPL unterscheidet NVML nicht in einzelne Domänen, sondern gibt vielmehr einen erhobenen Messwert für eine gesamte Einheit wie eine Grafik- oder Netzwerkkarte aus. NVML selbst ist eine C-API, die mit dem entsprechenden Treiber des jeweiligen Hardwarebeschleunigers ausgeliefert wird. Dazu erhalten wir in klassischer Nvidia-Tradition eine vorkompilierte Shared-Library und einen Header, mit dem die Anwendungen zur Energieverwaltung implementiert werden sollen.

Ein weiterer deutlicher Unterschied zu RAPL ist die zeitliche Auflösung, die mit einem Bereich von mehreren hundert Millisekunden etwa ein hundertstel der Messdichte von RAPL bietet.

Es lassen sich ebenfalls Powerlimits definieren, die das Energieaufnahmeverhalten der Karten zwingen können, sich an ein gesetztes Powerbudget zu halten. Außerdem können auch andere Metriken erhoben werden, wie beispielsweise die Auslastung einzelner Komponenten wie VRAM oder SIMD-Hardwareeinheiten. Sollten innerhalb eines Systems mehrere Grafikkarten verbaut sein, so bietet es sich an, das MIG (Multi-Instance GPU)-System zu verwenden, mithilfe dessen eine sinnvolle Datenaggregation vorgenommen wird.
\subsection{Slurm PrEp-Plugins}
Das Clustermanagementsystem Slurm bietet mit der PrEP-Plugin-Schnittstelle eine Möglichkeit, Anwendungen zu schreiben, die speziell zu Beginn und beim Abschluss eines Slurm-Jobs ausgeführt werden. Dabei wird das Programmierkonzept des Interfaces verwendet, bei dem vom Framework ein Funktionsplatzhalter definiert wird, der dann vom entsprechenden Plugin und somit vom Nutzer mit der eigenen Programmlogik überschrieben wird. Die Slurm-Plugin-Laufzeit muss also nicht direkt die entsprechende Erweiterungslogik beinhalten, sondern weiß nur, wo diese Logik liegt und wie sie entsprechend aufgerufen werden kann.

Slurm definiert hier im wesentlichen 4 entscheidende Funktionsvorlagen:
\begin{itemize}
    \item \mintinline{c}{int prep_p_prolog(job_env_t *job_env, slurm_cred_t *cred)} ist die Funktion, die von allen beteiligten Slurm-Knoten vor dem Start eines Jobs ausgeführt wird.
    \item \mintinline{c}{int prep_p_epilog(job_env_t *job_env, slurm_cred_t *cred)} wird am Ende eines Jobs auf allen Slurm-Knoten ausgeführt.
    \item \mintinline{c}{int prep_p_prolog_slurmctld(job_record_t *job_ptr, bool *async)} \newline ist im Prinzip analog zum regulären Prolog, wird jedoch nur auf dem Kopfknoten, also dem Controller, beim Start eines Jobs ausgeführt.
    \item \mintinline{c}{int prep_p_epilog_slurmctld(job_record_t *job_ptr, bool *async)} \newline ist der entsprechende Epilog zum Abschluss eines Jobs auf dem Kopfknoten, der bei Abschluss eines Jobs ausgeführt wird.
\end{itemize}
Diese 4 Funktionen werden, wie im folgenden Kapitel beschrieben, verwendet, um die Energiemessungen an den Knoten vorzunehmen.

Bevor ein Slurm-PrEp-Plugin verwendet werden kann, müssen drei wesentliche Datenfelder gesetzt werden:
\begin{itemize}
    \item \mintinline{c}{const char plugin_name[]="full text name"} definiert den vollständigen Plugin Namen.
    \item \mintinline{c}{const char plugin_type[]="major/minor"} definiert den Plugin-Typ. Major muss bei PrEp-Plugins immer \texttt{prep} sein und minor kann ein Kürzel des Plugin-Namens sein.
    \item \mintinline{c}{const uint32_t plugin_version} definiert die aktuelle Plugin-Version und erlaubt es Slurm, neuere Versionen zu erkennen und ein explizites Aktualisieren zu implementieren.
\end{itemize}
Abschließend kann der Plugin-Quelltext gebaut werden. Also sollte der zentrale Include 
\begin{itemize}
    \item \mintinline{c}{#include <src/interfaces/prep.h>}
\end{itemize} verwendet werden, da dieser Header die entsprechenden Funktionsdeklarationen seitens Slurm vornimmt.
\subsection{EPS}
EPS ist ein Slurm-PrEp-Plugin der PERFACCT GmbH, welches die bis hierher erläuterten Mechanismen und Frameworks vereint. Das Plugin erlaubt eine verteilte Energiemessung mittels EMA. EPS verwendet die Energiemessung mittels Intels RAPL und der sysfs-Schnittstelle. Außerdem gibt es eine GPU-Integration, die zusätzlich auch die NVML-API verwendet und den Energieverbrauch der Grafikkarten messen kann. Zu dem EPS Projekt gehören eine Reihe von Github-Repositories, wobei zwei von besonderer Bedeutung sind:
\begin{itemize}
    \item \href{https://github.com/PERFACCT/EPS}{https://github.com/PERFACCT/EPS} ist das Hauptrepository, in dem die Slurm-Plugin-Implementierung liegt.
    \item \href{https://github.com/PERFACCT/EPS-DB}{https://github.com/PERFACCT/EPS-DB} beinhaltet das Datenbankschema der Postgres-Datenbank auf dem Slurm-Control-Knoten, in dem die Datenaggregation der Energiemessung vorgenommen wird.
\end{itemize}
EPS soll als PrEp-Plugin auf allen Slurm-Knoten installiert werden und muss als Konfiguration den \texttt{EPS\_DB\_CONN\_STR} übergeben bekommen. Dieser String regelt, in welche Tabelle und insbesondere mit welcher Berechtigung auf die Datenbank zugegriffen werden kann. Somit sind also auch die Datenbank-Credentials in diesem String enthalten. 

Das im Rahmen dieses Projektes entwickelte Plugin EPSA kann als Weiterentwicklung von EPS betrachtet werden und startete somit auch mit derselben Code-Grundlage.
\section{Architektur von EPSA}
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{img/epsa-arch.pdf}
    \caption{EPSA-Architektur-Diagramm}
    \label{fig:placeholder}
\end{figure}
Die Erweiterung des EPS-Slurm-PrEp-Plugins namens EPSA wurde so konzipiert, dass am grundlegenden Gerüst der EPS-Erweiterung grundsätzlich nur minimale Änderungen vorgenommen werden, die einzig und allein die neue Funktionalität einführen. Dinge wie Code-Qualität und weitere Refactoring-Schritte wurden nicht durchgeführt. 

In Abbildung 4.1 ist die allgemeine Struktur von EPSA dargestellt. Dabei ist die Unterscheidung zwischen dem eigentlichen PrEp-Plugin und dem Anknüpfenden \texttt{awareness\_daemon} wichtig. Das EPSA-Plugin wird weiterhin in der Slurm-Umgebung geladen und ausgeführt und hat die Funktion, die Energiemessdaten von EMA abzuholen. Daraufhin werden mittels direkter Datenbankzugriffe die jeweiligen Datenbankeinträge vorgenommen. Dabei wurde die Slurm-Semantik größtenteils übernommen und zwischen Job und Execution unterschieden. Neu ist allerdings, dass der Unix-User, der den Job startet, ebenfalls vom EPSA-Plugin erfasst wird und EPSA überprüft, ob es einen entsprechenden User bereits in der Datenbank gibt. Ist dies nicht der Fall, so wird ein neuer Eintrag mit dem User erstellt und eine entsprechende Email-Adresse generiert. Diese wird später für das Versenden der Awareness-Nachrichten benötigt. Zusätzlich wurde der Epilog auf dem Slurm-Kopfknoten derart angepasst, dass die erhobenen Energiedaten neben den normalen Slurm-Log-Dateien als separate Log-Datei abgelegt werden. Der Nutzer kann somit nach Ausführung des Jobs direkt einsehen, wie viel Energie verbraucht wurde.

Auf der rechten Seite der Abbildung 4.1 ist der \texttt{awareness\_daemon} zu sehen, welcher mithilfe von YAML-Dateien konfiguriert werden kann. Der Daemon wurde mithilfe von UV implementiert und erlaubt somit eine weitestgehend plattformagnostische Ausführung. Ist der Daemon gestartet, so wird ein Thread gestartet, der periodisch die Energiemessdaten der in der Datenbank eingetragenen Nutzer sammelt und dann einen Bericht an die entsprechenden Nutzer versendet. Dazu werden Emails versendet. Die Email-Adressen der jeweiligen Nutzer werden ebenfalls in der User-Tabelle in der Postgres-Datenbank festgehalten. Per Standardkonfiguration wird eine Email-Adresse aus Unix-User-Name + '@uni-potsdam.de' erstellt. Außerdem wird ein Frontend gestartet, das die Energiemessdaten benutzerspezifisch visualisiert (siehe Abbildung 4.2).
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{img/Screenshot 2026-09-20 at 11-18-11 Dashboard – University of Potsdam Energy Awareness Daemon.png}
    \caption{Weboberfläche des \texttt{awareness\_daemon}}
    \label{fig:placeholder}
\end{figure}

Der \texttt{awareness\_daemon} führt nur lesende Zugriffe auf die Datenbank aus. Somit könnte ein spezieller Postgres-User mit nur den entsprechenden Berechtigungen erstellt werden. Aktuell sucht der \texttt{awareness\_daemon} im Slurm-Log nach dem entsprechenden Credential-String, um auf die Datenbank zugreifen zu können.
\section{Implementierung}
Im folgenden Kapitel werden die Hauptänderungen der EPSA-Erweiterung sowie zentrale Bestandteile des \texttt{awareness\_daemon} vorgestellt.
\subsection{Slurm Plugin}
Zu Beginn des Projektes wurden zunächst einige Änderungen vorgenommen, damit EPSA überhaupt mittels der neuen Slurm-Bibliotheken gebaut werden kann. Das EPS-Plugin, auf dem EPSA basiert, wird seit geraumer Zeit nicht mehr weiterentwickelt und hat somit auch nicht die Anpassungen modernerer Slurm-Versionen, vor allem an Datenstrukturen, vorgenommen. Daraufhin wurden die erste Funktionalität des Energiemessungs-Threads sowie die Ausgabe an den Nutzer mittels File-Writes implementiert, die im Folgenden vorgestellt werden (Code gekürzt):
\begin{minted}[xleftmargin=20pt,linenos]{c}
static void* epilog_energy_thread(void* arg) {
    PGconn* db_connection = connect_db();
    /* ... */
    while (1) {
        get_measurement_count(
            db_connection, job_id, &measurement_count
        );
        if (measurement_count > 0) break;
        if (waited_us >= EPILOG_SLURMCTLD_POLL_TIMEOUT_US) {
            slurm_info(
                "warning: no measurements found for JobId=%u,"
                "logging with whatever is available",
                job_id, waited_us / 1000
            );
            break;
        }
        /* ... */
    }
    uint64_t total_energy = 0;
    get_total_energy_consumption(
        db_connection, job_id, &total_energy
    );
    FILE* f = fopen("/var/log/slurm/eps_energy.log", "a");
    fprintf(
        f, "Job %u: Total energy consumption: %.6f kWh\n",
        job_id, (double)total_energy / 3.6e12
    );
    fclose(f);
    PQfinish(db_connection);
}
\end{minted}
Der hier gezeigte Energy-Thread wird im Epilog des Slurm-Kopfknotens gestartet und wartet so lange, bis die Entsprechenden Einträge in der Datenbank vorgenommen wurden (siehe Zeile 8 bis 17). Hintergrund ist, dass EMA selbst einen Thread zum Eintragen der Werte verwendet. Die PrEp-API ist blockierend, weswegen solche I/O-Aufgaben grundsätzlich asynchron durchgeführt werden sollten.

Die im vorherigen Kapitel erwähnte Anpassung der Datenbankstruktur bedarf einer neuen Funktion zum Einfügen eines Nutzers in die users-Tabelle. Dazu wurde eine entsprechende Änderung in \texttt{eps\_db.c} vorgenommen (Code gekürzt):
\begin{minted}[xleftmargin=20pt,linenos]{c}
int insert_user_data(
        PGconn* connection, int userid, const char* username) {
    uint32_t bin_userid = htobe32((uint32_t) userid);
    char user_email[256];
    snprintf(
        user_email, sizeof(user_email), "%s@uni-potsdam.de", username
    );
    /* ... */
    PQexecParams(
        connection,
        "INSERT INTO users ('USER_COLS') VALUES($1, $2, $3) ",
        /* ... */
    );
    return check_query_result(res, connection);
}
\end{minted}
Wie in den Zeilen 2 bis 4 zu sehen ist, wird die Nutzer-E-Mail aktuell hart mit dem Suffix der Universität Potsdam gesetzt. Sollte EPSA in einem anderen Kontext eingesetzt werden, ist an dieser Stelle natürlich eine Anpassung vorzunehmen. Gegebenenfalls ist es hier auch sinnvoll, einen eigenen Generierungs-Algorithmus zu implementieren, da nicht alle Email-Systeme die gleiche Semantik wie an der Universität Potsdam verwenden.

Weitere zentrale Änderungen Umfassen die Anpassung der Datenbank-Funktionen zum Eintragen der Messdaten. Diese wurden um die \texttt{userid} ergänzt, um ein relationales Datenbankschema zu erstellen.
Kleinere Änderungen können im Github-Repository mittels eines \texttt{git diff} eingesehen werden.
\newpage
\subsection{awareness\_daemon}
Das \texttt{awareness\_daemon} bildet die größte Neuerung von EPSA. Er bedient im wesentlichen 4 zentrale Funktionen:
\begin{itemize}
    \item \textbf{Sammlung der Nutzerdaten} in einem vorher definierten zeitlichen Intervall.
    \item \textbf{Weboberfläche} zum einfachen einsehen des Energieverbrauchs im Vergleich zu anderen Nutzer*innen.
    \item \textbf{Email-Client} zum versenden der monatlichen Energieberichte an die Nutzer*innen.
    \item \textbf{Comparator} zum erstellen eines sinnvollen Energieverbrauchsvergleichs.
\end{itemize}
Die Sammlung der Nutzerdaten geschieht über einen Thread, der von der \texttt{awareness\_daemon}-Anwendung gestartet wird. Der \texttt{awareness\_daemon} überprüft zyklisch, ob der entsprechende Thread noch lebt, und startet ihn ggf. mit einem definierten Backoff-Timer neu.

Das Web-Frontend wird mittels eines Flask-Servers bereitgestellt.

Der Email-Client verwendet das Python-Pip-Paket namens \texttt{email}, und die entsprechenden Nachrichten zum Energieverbrauch werden wie folgt generiert:
\begin{minted}[xleftmargin=20pt,linenos]{python}
def _build_message(self, user):
    device_name, device_time = self.comparator.compare_consumption(
        user.get_last_week_energy()
    )
    body = (
        f"Hi {user.username},\n\n"
        f"Your total energy consumption over the past week is "
        f"{user.get_last_week_energy():.4f} kWh"
        f"({user.get_last_week_energy() * 
            self.config.get('misc', {})
                .get('energy_price_per_kwh', 0):.2f}" 
        f"{self.config.get('misc', {}).get('currency', '€')}).\n"
        f"That is roughly equivalent to running"
        " a {device_name} for {device_time}.\n\n"
        f"Your overall energy consumption is "
        "{user.total_energy_kwh:.4f} kWh"
        "({user.total_energy_kwh * 
            self.config.get('misc', {})
                .get('energy_price_per_kwh', 0):.2f}"
        " {self.config.get('misc', {}).get('currency', '€')}).\n\n"
        f"Cheers,\n"
        f"{self.config.get('server', {})
            .get('instance_name', 'EPSA Awareness Dashboard')}\n"
    )
    return (user.email, "Your energy consumption this week", body)
\end{minted}
Wie in Zeile 2 bis 4 zu sehen ist, wird anhand der vom Nutzer verbrauchten Energie ein Gerät und eine entsprechende Laufzeit mithilfe der \mintinline{python}{Comparator()}-Klasse berechnet. Diese Klasse ist im Kern die Hauptfunktionalität des gesamten Projekts, da mit ihrer Hilfe aus einer Liste von vorher definierten elektrischen Haushaltsgeräten eines zufällig ausgewählt wird. Daraufhin wird der entsprechende Verbrauch zusammen mit dem Gerätenamen als Tupel zurückgegeben und kann in die entsprechenden Interfaces eingebaut werden. Die erwähnte Vergleichsfunktion sieht wie folgt aus:
\begin{minted}[xleftmargin=20pt,linenos]{python}
def compare_consumption(self, consumption_in_kwh):
    close_devices = []
    consumption_in_watthours = 
        consumption_in_kwh * 1000  # Convert kWh
    min_time_seconds = 10

    for device, watts in self.consumers_in_watts:
        required_time_seconds = 
            consumption_in_watthours / watts * 3600
        if min_time_seconds <= required_time_seconds:
            close_devices.append((device, required_time_seconds))

    if not close_devices:
        close_devices = [
            (device, consumption_in_watthours / watts * 3600)
            for device, watts in self.consumers_in_watts
        ]

    compare_device = 
        close_devices[random.randint(0, len(close_devices) - 1)]

    return compare_device[0], 
        self.sensible_time_output(compare_device[1])
\end{minted}
Als Eingabe übergibt man den direkten Verbrauch in kWh. Dazu wird eine Liste mit den nächstgelegenen Verbrauchern erstellt und dann zufällig aus dieser ausgewählt. Damit die Textausgabe nicht nur die Sekunden direkt, sondern eine sinnvollere Ausgabe wie '1 hour 4 minutes und 32 seconds' ausgibt, wurde zusätzlich eine Klassenmethode hinzugefügt.
\begin{itemize}
    \item \mintinline{python}{Comparator.sensible_time_output()}
\end{itemize}
Um eine elegantere Programmierschnittstelle anbieten zu können, wurde eine Datenklasse 
\begin{itemize}
    \item \mintinline{python}{EPSAUser()}
\end{itemize} 
implementiert, die von entsprechenden Zugriffsfunktionen aus der
\begin{itemize}
    \item \mintinline{python}{DatabaseHandler()}-Klasse
\end{itemize} 
instanziiert werden. Die Comparator-Klasse wird sowohl vom Email-Client als auch vom Webinterface zur Erstellung des Output-Textes zum Energieverbrauch verwendet.
\section{Limitationen}
Die Limitation des Projektes ist vor allem die Bindung des Programms an die Datenbank. Sollte es dort zu einem Fehler oder Ausfall kommen, ist der gesamte Service blockiert. Im Entwicklungsprozess ist dies mehrmals vorgekommen und hat außerdem dazu geführt, dass Jobs im Slurm-Cluster nicht mehr beendet werden konnten und somit das Cluster in einem Fehlerzustand verweilte. Dies kann lange Debugging-Zyklen nach sich ziehen und wäre im Optimalfall natürlich durch eine Fehlerresilienz auszugleichen oder abzufangen.

Programmspezifisch erlaubt EPSA außerdem, genauso wie EPS, kein Energieprofiling in Code-Bereichen. Dies ist eines der Kern-Features von EMA und wäre natürlich sehr hilfreich bei der Identifikation von Energieintensiven Bereichen in einer verteilten Anwendung im Slurm-Kontext. Eine derartige Implementierung würde jedoch eine Reihe von Features benötigen und wäre mit erheblichem Programmieraufwand verbunden.

Zuletzt sei erwähnt, dass es nicht möglich ist, Energiemessungen von abgebrochenen oder fehlerhaft beendeten Jobs zu erheben. Dies ist der Tatsache geschuldet, dass Energiemessungen erst mit dem Durchlauf der Epilog-Funktionen abgelegt werden, die insbesondere nicht aufgerufen werden, wenn das Programm eines Jobs vorzeitig beendet wird.
\newpage
\section{Deployment}
Da das Projekt EPSA von Anfang an mit dem Ziel, in Produktion eingesetzt zu werden, entwickelt wurde, wurde stets versucht, eine reproduzierbare und wartbare Architektur zu schaffen.

Zunächst muss auf allen Knoten das EPSA-PrEp-Plugin installiert werden. Dazu kann das Github-Repository geforkt werden und mit dem entsprechenden CMakeFile gebaut werden. Als Ergebnis wird eine Shared-Library erstellt, die dann an den entsprechend von Slurm konfigurierten Plugin-Pfaden hinterlegt werden muss. Außerdem benötigt jeder Knoten die notwendigen Postgres-Bibliotheken, die vorzugshalber mithilfe des jeweiligen Betriebssystem-Paketmanagers installiert werden sollten. Dabei ist auf die korrekte Erstellung des Credential-Strings der Postgres-Datenbank zu achten, da diese von den jeweiligen Plugin-Instanzen sowie dem \texttt{awareness\_daemon} benötigt werden.

Um den Build-Prozess möglichst Entwickler*innenfreundlich zu gestalten, wurde ein entsprechendes Skript zum bauen des Plugins erstellt. 
Es kann wie folgt aufgerufen werden:
\begin{minted}{bash}
./build.sh
\end{minted}
Um den \texttt{awareness\_daemon} zu installieren, sollte folgendes ausgeführt werden:
\begin{minted}{bash}
cd awareness_daemon
uv tool install .
\end{minted}
Dies installiert das \texttt{awareness\_daemon} an der korrekten, von UV voreingestellten Stelle. Im Pfad des \texttt{awareness\_daemon} liegen außerdem zwei Konfigurationsdateien:
\begin{itemize}
    \item \texttt{awareness-daemon-config.yaml} beinhaltet allgemeine Serverkonfigurationen wie Instanz-Name, Server-Adresse und Server-Port. Außerdem kann der Energiepreis sowie die Landeswährung angepasst werden.
    \item \texttt{consumers.yaml} beinhaltet eine Liste von Energieverbrauchern mit deren Namen und dem entsprechenden Wattverbrauch.
\end{itemize}
Diese Konfigurationen sollten vor inbetriebnahme entsprechend angepasst werden, sind allerdings mit Default-Werten belegt, die der Testumgebung des Clusters an der Universität Potsdam entsprechen.
\newpage
\section{Fazit}

\newpage
\printbibliography

\end{document}